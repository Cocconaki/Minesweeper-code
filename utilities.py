import settings 

def calculating_precentage(precentage):
    return (settings.WIDTH / 100) * precentage

def calculating_height_precentage(precentage):
    return (settings.HEIGHT / 100) * precentage

